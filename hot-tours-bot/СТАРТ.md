# СТАРТ — Запуск Family Tour Bot

Это пошаговая инструкция для запуска бота с нуля.
Код уже написан. Твоя задача — только заполнить токены и нажать запуск.

---

## Что нужно сделать ОДИН РАЗ (подготовка)

### 1. Установи Python

1. Зайди на [python.org/downloads](https://python.org/downloads)
2. Скачай Python **3.11** или новее
3. При установке **обязательно** поставь галочку **"Add Python to PATH"**

Проверь в терминале VS Code (`Ctrl + тильда`):
```
python --version
```
Должно показать: `Python 3.11.x`

---

### 2. Открой папку проекта в терминале

```bash
cd "c:/Users/Пользователь/Documents/Проекты/moi_idei/hot-tours-bot"
```

---

### 3. Создай виртуальное окружение

> Это изолированная папка с библиотеками для этого проекта. Делается один раз.

```bash
python -m venv venv
venv\Scripts\activate
```

После активации в начале строки появится `(venv)` — это нормально.

---

### 4. Установи зависимости

```bash
pip install -r requirements.txt
```

Займёт 1–2 минуты. Один раз.

---

### 5. Создай файл настроек

```bash
copy .env.example .env
```

Открой `.env` в VS Code. Заполни токены — см. таблицу ниже.

---

## Таблица токенов — где взять каждый

| Переменная | Где получить | Обязательно |
|------------|-------------|-------------|
| `TOURVISOR_LOGIN` | Твой email на tourvisor.ru | ✅ |
| `TOURVISOR_PASSWORD` | Твой пароль на tourvisor.ru | ✅ |
| `TELEGRAM_BOT_TOKEN` | @BotFather → `/newbot` → скопировать токен | ✅ |
| `TELEGRAM_CHANNEL_ID` | @username канала или `-100XXXXXXXXX` | ✅ |
| `TELEGRAM_ADMIN_ID` | Написать боту @userinfobot — пришлёт твой ID | ✅ |
| `VK_TOKEN` | [vk.com/dev](https://vk.com/dev) → Мои приложения → ключ доступа группы | для ВК |
| `VK_GROUP_ID` | Числовой ID группы family_toor (без минуса) | для ВК |
| `ANTHROPIC_API_KEY` | [console.anthropic.com](https://console.anthropic.com) → API Keys | для ИИ-текстов |
| `GOOGLE_CREDENTIALS_FILE` | JSON-ключ сервисного аккаунта Google (см. ниже) | для базы |
| `GOOGLE_SHEET_ID` | ID из URL таблицы Google Sheets | для базы |

---

## Как создать Telegram-бота (5 минут)

1. Открой Telegram → найди **@BotFather**
2. Напиши `/newbot`
3. Придумай название: `Family Tour Уфа`
4. Придумай username (латиницей, оканчивается на bot): `familytour_ufa_bot`
5. BotFather пришлёт токен вида `7123456789:AAF...`
6. Скопируй в `.env` → `TELEGRAM_BOT_TOKEN=...`

---

## Как настроить Google Sheets (10 минут)

1. Зайди на [console.cloud.google.com](https://console.cloud.google.com)
2. Создай новый проект (любое название)
3. Включи два API: **Google Sheets API** и **Google Drive API**
   - Поиск по "Google Sheets API" → Включить
4. Создай сервисный аккаунт:
   - IAM и администрирование → Сервисные аккаунты → Создать
   - Скачай JSON-ключ → сохрани как `google_credentials.json` в папку проекта
5. Создай новую Google Sheets таблицу
6. Поделись таблицей с email сервисного аккаунта (из JSON-файла) как **Редактор**
7. Скопируй ID таблицы из URL и вставь в `.env` → `GOOGLE_SHEET_ID=...`

---

## Запуск по шагам (проверяем что всё работает)

### Шаг 1 — Tourvisor API

```bash
python test_step1.py
```

**Ожидаемый результат:** список горящих туров с ценами в консоли.

Если ошибка:
- Проверь `TOURVISOR_LOGIN` и `TOURVISOR_PASSWORD` в `.env`
- Убедись что аккаунт на tourvisor.ru активен

---

### Шаг 2 — Генерация поста

```bash
python test_step2.py
```

**Ожидаемый результат:** готовый текст поста в консоли.

- С `ANTHROPIC_API_KEY` → живой текст от Claude AI
- Без ключа → шаблонный текст (тоже работает)

---

### Шаг 3 — Публикация

```bash
python test_step3.py
```

**Ожидаемый результат:**
- В Telegram-канале появился пост
- В ВК появился пост
- Тебе пришло превью на одобрение

---

### Запуск всей системы

```bash
python main.py
```

Бот работает 24/7. Публикации в **09:30 / 14:00 / 19:00** автоматически.

Чтобы остановить: `Ctrl + C`

---

## Режимы работы

| Режим | Как | Что происходит |
|-------|-----|----------------|
| **Одобрение** (неделя 1) | `APPROVAL_MODE = True` в `main.py` | Бот присылает превью → ты нажимаешь ✅ или ❌ |
| **Автопилот** (со 2й недели) | `APPROVAL_MODE = False` | Бот публикует сам, присылает уведомление |

---

## Структура файлов

```
hot-tours-bot/
├── СТАРТ.md                  ← эта инструкция
├── .env.example              ← шаблон настроек (шаблон, не трогать)
├── .env                      ← твои реальные токены (НЕ показывать никому!)
├── .gitignore                ← .env и ключи Google скрыты от git
├── requirements.txt          ← список библиотек Python
├── config.py                 ← читает .env и раздаёт настройки
├── main.py                   ← ЗАПУСК СИСТЕМЫ (python main.py)
│
├── tourvisor/
│   └── client.py             ← подключается к Tourvisor, ищет туры
│
├── ai/
│   └── generator.py          ← генерирует текст поста через Claude AI
│
├── publisher/
│   ├── telegram.py           ← публикует в Telegram + уведомления
│   └── vk.py                 ← публикует в ВКонтакте
│
├── bot/
│   └── handler.py            ← диалог сбора заявок от клиентов
│
├── sheets/
│   └── client.py             ← сохраняет заявки в Google Sheets
│
├── test_step1.py             ← тест: Tourvisor API
├── test_step2.py             ← тест: генерация поста
└── test_step3.py             ← тест: публикация в соцсети
```

---

## Если что-то не работает

1. Убедись что в начале строки есть `(venv)` — если нет, запусти `venv\Scripts\activate`
2. Убедись что заполнен `.env`, а не `.env.example`
3. Скинь текст ошибки — разберёмся вместе

---

*Обновлено: 09.04.2026*
