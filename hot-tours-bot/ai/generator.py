"""
ai/generator.py — Генератор текста поста через Claude API

Берёт сухие данные тура (отель, цена, питание...)
и превращает их в живой, привлекательный пост для ВК/Telegram.

Claude Haiku — самая дешёвая модель, стоит ~0.1₽ за пост.
"""

import anthropic
import random
import re
from tourvisor.client import Tour


# Шаблон поста — Claude заполняет [ОПИСАНИЕ] и [ПРЕИМУЩЕСТВА]
POST_TEMPLATE = """<b>🔥 ГОРЯЩИЙ ТУР! {country}</b>

✈️ Вылет: {date_from} из {city_from} ({nights} {nights_word})
🏨 <b>{hotel_name}</b> {stars}
🍽 Питание: {meal}
{sea_line}
💰 Цена: <b>{price_per_person}/чел</b>

{ai_description}

{ai_advantages}

📩 Написать нам: <b>@hottourpegas_bot</b>
📞 Позвонить: <b>+7 (917) 044-21-00</b>
⚡ Количество мест ограничено!

#горящийтур #{country_tag}"""


# Перевод кодов питания на русский язык
MEAL_RU = {
    "AI":                   "Всё включено",
    "ALL":                  "Всё включено",
    "All Inclusive":        "Всё включено",
    "UAI":                  "Ультра всё включено",
    "Ultra All Inclusive":  "Ультра всё включено",
    "HB":                   "Полупансион",
    "Half Board":           "Полупансион",
    "FB":                   "Полный пансион",
    "Full Board":           "Полный пансион",
    "BB":                   "Завтрак",
    "Bed & Breakfast":      "Завтрак",
    "Bed and Breakfast":    "Завтрак",
    "RO":                   "Без питания",
    "Room Only":            "Без питания",
    "SC":                   "Без питания",
}


def _meal_ru(meal) -> str:
    """Возвращает русское название типа питания, если код известен."""
    meal = str(meal).strip()
    return MEAL_RU.get(meal, meal)


def _nights_word(n: int) -> str:
    """Склонение слова 'ночь': 7 ночей, 1 ночь, 3 ночи"""
    if 11 <= n % 100 <= 14:
        return "ночей"
    r = n % 10
    if r == 1:
        return "ночь"
    if 2 <= r <= 4:
        return "ночи"
    return "ночей"


def _country_tag(country: str) -> str:
    """Превращает название страны в хэштег: 'ОАЭ' → 'ОАЭ'"""
    return country.replace(" ", "_")


# Винительный падеж (куда? — в Турцию, в Россию)
COUNTRY_ACCUSATIVE = {
    "Россия":       "Россию",
    "Турция":       "Турцию",
    "Египет":       "Египет",
    "ОАЭ":          "ОАЭ",
    "Таиланд":      "Таиланд",
    "Мальдивы":     "Мальдивы",
    "Куба":         "Кубу",
    "Доминикана":   "Доминикану",
    "Индия":        "Индию",
    "Шри-Ланка":    "Шри-Ланку",
    "Индонезия":    "Индонезию",
    "Вьетнам":      "Вьетнам",
    "Грузия":       "Грузию",
    "Абхазия":      "Абхазию",
    "Беларусь":     "Беларусь",
    "Армения":      "Армению",
    "Азербайджан":  "Азербайджан",
    "Узбекистан":   "Узбекистан",
    "Казахстан":    "Казахстан",
    "Киргизия":     "Киргизию",
    "Марокко":      "Марокко",
    "Тунис":        "Тунис",
    "Иордания":     "Иорданию",
    "Израиль":      "Израиль",
    "Кипр":         "Кипр",
    "Греция":       "Грецию",
    "Италия":       "Италию",
    "Испания":      "Испанию",
    "Черногория":   "Черногорию",
    "Хорватия":     "Хорватию",
    "Болгария":     "Болгарию",
    "Сербия":       "Сербию",
    "Венгрия":      "Венгрию",
    "Чехия":        "Чехию",
    "Австрия":      "Австрию",
    "Франция":      "Францию",
    "Португалия":   "Португалию",
    "Малайзия":     "Малайзию",
    "Сингапур":     "Сингапур",
    "Филиппины":    "Филиппины",
    "Сейшелы":      "Сейшелы",
    "Маврикий":     "Маврикий",
    "Танзания":     "Танзанию",
    "Кения":        "Кению",
    "ЮАР":          "ЮАР",
    "Мексика":      "Мексику",
    "Куба":         "Кубу",
    "Ямайка":       "Ямайку",
    "Бразилия":     "Бразилию",
    "Аргентина":    "Аргентину",
    "Перу":         "Перу",
    "Китай":        "Китай",
    "Япония":       "Японию",
    "Южная Корея":  "Южную Корею",
    "Камбоджа":     "Камбоджу",
    "Лаос":         "Лаос",
    "Мьянма":       "Мьянму",
    "Непал":        "Непал",
    "Бутан":        "Бутан",
}


def _country_acc(country: str) -> str:
    """Возвращает страну в винительном падеже (куда? — в Турцию)."""
    return COUNTRY_ACCUSATIVE.get(country.strip(), country)


# ── Пулы случайных преимуществ ──────────────────────────────────

_ADV_MEAL_AI = [
    "✅ Всё включено — еда, напитки, развлечения",
    "✅ Система All Inclusive — ни о чём не беспокойтесь",
    "✅ Питание и напитки на весь отдых — включено",
    "✅ Ресторан, бар, перекусы — всё уже оплачено",
    "✅ Можно ни разу не доставать кошелёк — всё включено",
]

_ADV_MEAL_UAI = [
    "✅ Ultra All Inclusive — по-настоящему ни в чём себе не отказывать",
    "✅ Премиум «всё включено» — алкоголь, рестораны à la carte",
    "✅ UAI — фирменные напитки и блюда от шеф-повара включены",
]

_ADV_MEAL_HB = [
    "✅ Полупансион — завтрак и ужин уже в цене",
    "✅ HB — начинайте день с завтрака, заканчивайте ужином",
    "✅ Кормят утром и вечером — днём свобода",
]

_ADV_MEAL_BB = [
    "✅ Завтрак включён — утро без забот",
    "✅ С утра — шведский стол, потом исследуйте город",
    "✅ Завтрак в отеле, днём — местная кухня",
]

_ADV_STARS_5 = [
    "✅ Отель {s}⭐ — премиум сервис и комфорт",
    "✅ {s}⭐ — настоящая роскошь на отдыхе",
    "✅ Люкс-категория {s}⭐ — обслуживание на высшем уровне",
    "✅ {s} звёзд — внимание к каждой мелочи",
]

_ADV_STARS_4 = [
    "✅ Отель {s}⭐ — высокий уровень комфорта",
    "✅ {s}⭐ — проверенное качество для отдыха",
    "✅ {s} звезды — всё необходимое для приятного отпуска",
    "✅ Солидные {s}⭐ — без сюрпризов",
]

_ADV_FROM_UFA = [
    "✅ Вылет прямо из Уфы — без пересадок",
    "✅ Рейс из родной Уфы — собрались и поехали",
    "✅ Из уфимского аэропорта — удобно и быстро",
    "✅ Уфа → отель без лишних пересадок",
    "✅ Вылет из Уфы — экономите время и силы",
]

_ADV_HOT_PRICE = [
    "✅ Горящая цена — не упустите момент",
    "✅ Цена падает прямо сейчас — успейте забронировать",
    "✅ Такое бывает редко — ловите, пока есть",
    "✅ Горящий тур = серьёзная экономия",
    "✅ Цена ниже обычной — только на горящих местах",
    "✅ Эта цена живёт часы — дальше дороже",
    "✅ Бронирование сейчас — лучшая цена этой недели",
]

_ADV_FAMILY = [
    "✅ Отлично подойдёт для отдыха с детьми",
    "✅ Детская анимация и бассейны для малышей",
    "✅ Семейный формат — интересно всем",
    "✅ С детьми, вдвоём или компанией — всем хорошо",
]

_ADV_SEA_CLOSE = [
    "✅ Первая линия — море за минуту пешком",
    "✅ Прямо у моря — не тратьте время на дорогу",
    "✅ До пляжа — пара минут, считайте каждую минуту солнца",
]

_ADV_BONUS = [
    "✅ Оформление за 1 день — быстро и без хлопот",
    "✅ Поддержка менеджера 24/7 — всегда на связи",
    "✅ Проверенный туроператор — никаких сюрпризов",
    "✅ Сопровождение до самого возвращения",
    "✅ Страховка уже включена в стоимость",
    "✅ Все документы подготовим за вас",
]


def _pick_advantages(meal: str, stars_digit: str, has_children_ok: bool = True,
                     sea_close: bool = False, count: int = 2) -> list[str]:
    """Случайно собирает набор преимуществ без дублей. По умолчанию 2 галочки."""
    # Все пулы из которых можем брать
    all_pools = [_ADV_FROM_UFA, _ADV_HOT_PRICE]

    meal_upper = meal.upper().strip() if meal else ""
    if meal_upper in ("UAI", "ULTRA ALL INCLUSIVE"):
        all_pools.append(_ADV_MEAL_UAI)
    elif meal_upper in ("AI", "ALL", "ALL INCLUSIVE"):
        all_pools.append(_ADV_MEAL_AI)
    elif meal_upper in ("HB", "HALF BOARD"):
        all_pools.append(_ADV_MEAL_HB)
    elif meal_upper in ("BB", "BED & BREAKFAST", "BED AND BREAKFAST"):
        all_pools.append(_ADV_MEAL_BB)

    if stars_digit and stars_digit.isdigit():
        s = int(stars_digit)
        if s >= 5:
            all_pools.append([t.format(s=s) for t in _ADV_STARS_5])
        elif s >= 4:
            all_pools.append([t.format(s=s) for t in _ADV_STARS_4])

    if sea_close:
        all_pools.append(_ADV_SEA_CLOSE)

    if has_children_ok:
        all_pools.append(_ADV_FAMILY)

    all_pools.append(_ADV_BONUS)

    # Выбираем count разных пулов и из каждого по одной случайной фразе
    chosen_pools = random.sample(all_pools, k=min(count, len(all_pools)))
    return [random.choice(pool) for pool in chosen_pools]


def _random_headline(country: str, hotel: str, nights: str,
                     price_str: str, meal_ru: str, stars_str: str) -> str:
    """
    Случайный цепляющий заголовок для поста.
    country   — страна в именительном падеже ("Турция")
    nights    — уже склонённая строка ("7 ночей" / "3 ночи")
    """
    country_acc = _country_acc(country)
    templates = [
        # ── Горячие / срочные ──
        f"🔥 Горящий тур в {country_acc} — мест почти нет!",
        f"⏰ Забронируйте за 10 минут — {country} уходит!",
        f"🚨 Внимание: {country} по смешной цене!",
        f"⚡ Последние места: {country}, {hotel}",
        f"🔥 Этот тур исчезнет к вечеру — {country} от {price_str}",

        # ── FOMO / страх упустить ──
        f"😱 Такую цену в {country_acc} вы ещё не видели!",
        f"💔 Упустите — пожалеете: {country}, {price_str}/чел",
        f"👀 Пока вы думаете — кто-то бронирует {country}",
        f"🎯 Не для всех: горящая цена на {country}",

        # ── Мечта / эмоции ──
        f"🤩 Мечтали о {country}? Вот ваш шанс!",
        f"💫 {country} зовёт — и не отпустит",
        f"🌊 Море, солнце, {meal_ru} — {country} ждёт!",
        f"🌴 {country} — там, где вы забудете про будни",
        f"✨ Чемодан, паспорт, улыбка — и {country} ваша",
        f"🌺 {country}: когда отдых превращается в сказку",

        # ── Инсайдерские / эксклюзив ──
        f"🤫 Нашли для вас: {hotel} в {country_acc}",
        f"💎 Тихая находка: {country}, {hotel} {stars_str}",
        f"🔑 Секретная цена: {country} от {price_str}/чел",
        f"🎁 Подарок дня — тур в {country_acc}",

        # ── Практические / про деньги ──
        f"💰 {country}: отдых без переплаты",
        f"💸 {country} от {price_str}/чел — считайте выгоду",
        f"🏷 Цена дня: {country}, {nights}, {meal_ru}",
        f"💫 Лучшая цена недели — {country}",
        f"🎯 Готовый вариант: {country}, всё включено (ну почти)",

        # ── Вопросы / обращение ──
        f"🤔 Куда в отпуск? Вот ответ — {country}",
        f"✈️ Улетаем в {country_acc}? Осталось несколько мест!",
        f"🏖 Надоела Уфа? Вот она — {country}",
        f"🌞 Соскучились по солнцу? {country} через пару дней",

        # ── Локальные / из Уфы ──
        f"✈️ Из Уфы в {country_acc} — прямой вылет, {nights}",
        f"🛫 Прямо из Уфы — {country}, {price_str}/чел",
        f"🏡 Без пересадок из Уфы — {country} на {nights}",

        # ── Про отель ──
        f"🏨 {hotel} {stars_str} — и это цена?!",
        f"😍 {hotel} {stars_str} в {country_acc} — сказочно выгодно",
        f"🎯 Нашли: {hotel} {stars_str}, {country}",
        f"💥 {hotel} в {country_acc} — глаза не верят цене",

        # ── Про питание / атмосферу ──
        f"🍹 {meal_ru}, море, {country} — вот это план",
        f"🥂 {country}: {meal_ru.lower()}, солнце, никаких забот",
        f"🍽 {country}, {meal_ru.lower()} — ни о чём не думать {nights}",

        # ── Интригующие / нестандартные ──
        f"😮 Три цифры цены — и вы в {country_acc}",
        f"🎉 Есть повод: {country} от {price_str}/чел",
        f"🌟 Пока все смотрят — мы нашли {country}",
        f"👇 Смотрите внимательно: {country}, {nights}",
        f"🍀 Повезло тем, кто первый: {country}",

        # ── Про время ──
        f"📅 Отпуск уже скоро: {country}, вылет {nights}",
        f"⌛ Пока отпуск не сгорел — летим в {country_acc}",
        f"🚀 Через пару дней — {country}. Вы готовы?",

        # ── Про семью / компанию ──
        f"👨‍👩‍👧 Всей семьёй — в {country_acc}",
        f"💑 Вдвоём в {country_acc}: {nights} на двоих",

        # ── Слоганы ──
        f"🌴 {country} — это не мечта, это реальная цена",
        f"🔑 Ваш билет на море: {hotel}, {country}",
        f"🙌 Наконец-то! {country} по цене, которую ждали",
        f"💃 Отдых в {country_acc} — дешевле чем думаете",
    ]
    return random.choice(templates)


def generate_post(tour: Tour, api_key: str) -> str:
    """
    Генерирует готовый текст поста для одного тура.

    Как работает:
    1. Собираем базовые данные из объекта Tour
    2. Отправляем Claude короткий запрос — он пишет описание и преимущества
    3. Подставляем в шаблон и возвращаем готовый текст

    Параметры:
        tour    — объект Tour с данными о туре
        api_key — ключ Claude API из .env
    """

    # ── Шаг 1: генерируем описание и преимущества через Claude ──
    client = anthropic.Anthropic(api_key=api_key)

    prompt = f"""Ты — копирайтер турагентства. Напиши для поста в ВК два блока на русском языке.

Данные отеля:
- Отель: {tour.hotel_name} {tour.hotel_stars}★
- Страна: {tour.country}, {tour.resort}
- Питание: {tour.meal_label}
- Расстояние до моря: {tour.distance_sea} м
- Ночей: {tour.nights}

Блок 1 — ОПИСАНИЕ (1-2 предложения, атмосферное, без воды):
Начни с новой строки, без заголовка. Например: "Роскошный отель первой линии с собственным пляжем..."

Блок 2 — ПРЕИМУЩЕСТВА (3-4 пункта, каждый с эмодзи ✅):
Конкретные факты об отеле. Например:
✅ Первая линия — 50 метров до пляжа
✅ Ultra All Inclusive — питание и напитки включены
✅ Аквапарк и SPA в отеле

Отвечай только двумя блоками, без лишних слов и заголовков."""

    message = client.messages.create(
        model="claude-haiku-4-5-20251001",  # самая быстрая и дешёвая модель
        max_tokens=400,
        messages=[{"role": "user", "content": prompt}],
    )

    ai_text = message.content[0].text.strip()

    # Разделяем на описание и преимущества по строке с ✅
    parts = ai_text.split("✅", 1)
    if len(parts) == 2:
        ai_description = parts[0].strip()
        ai_advantages  = "✅" + parts[1].strip()
    else:
        # Если Claude не разделил как надо — берём весь текст как описание
        ai_description = ai_text
        ai_advantages  = ""

    # ── Шаг 2: подставляем в шаблон ──────────────────────────
    sea_line = f"🌊 До моря: {tour.distance_sea} м" if tour.distance_sea else ""

    post = POST_TEMPLATE.format(
        country          = tour.country,
        date_from        = tour.date_from,
        city_from        = tour.city_from,
        nights           = tour.nights,
        nights_word      = _nights_word(tour.nights),
        hotel_name       = tour.hotel_name,
        stars            = tour.stars_str,
        meal             = _meal_ru(tour.meal_label),
        sea_line         = sea_line,
        price_per_person = tour.formatted_price_per_person,
        ai_description   = ai_description,
        ai_advantages    = ai_advantages,
        country_tag      = _country_tag(tour.country),
    )

    return post


def generate_post_from_dict(data: dict, api_key: str = "") -> str:
    """
    Генерирует пост из словаря с данными тура (из Google Sheets).

    data — строка из листа 'Туры к публикации':
        Страна, Курорт, Отель, Звёзды, Питание,
        Дата вылета, Ночей, Цена/чел, Ссылка

    Если api_key не задан — генерирует по шаблону без ИИ.
    """
    country  = str(data.get("Страна", "") or "")
    resort   = str(data.get("Курорт", "") or "")
    hotel    = str(data.get("Отель", "") or "")
    stars    = ""  # столбец Звёзды удалён из таблицы
    meal     = str(data.get("Питание", "") or "")
    date     = str(data.get("Дата вылета", "") or "")
    nights   = str(data.get("Ночей", "") or "")
    price    = str(data.get("Цена/чел", "") or "")
    link     = str(data.get("Ссылка", "") or "")

    stars_str  = f"{'⭐' * int(stars)}" if stars.isdigit() else stars
    meal_ru    = _meal_ru(meal)
    nights_str = f"{nights} {_nights_word(int(nights))}" if nights.isdigit() else f"{nights} ночей"
    # Очищаем цену — оставляем только цифры и точку
    price_clean = re.sub(r"[^\d.]", "", price)
    price_str   = f"{int(float(price_clean)):,}".replace(",", " ") + " ₽" if price_clean else "уточняйте"
    link_line  = f"\n🔗 Подробнее: {link}" if link else ""
    headline   = _random_headline(country, hotel, nights_str, price_str, meal_ru, stars_str)

    if api_key:
      try:
        country_acc = _country_acc(country)
        client = anthropic.Anthropic(api_key=api_key)
        prompt = f"""Ты — копирайтер турагентства «Family Tour» (Уфа). Напиши продающий пост для Telegram с HTML-разметкой.

Данные тура:
- Страна (именительный): {country}
- Страна (винительный — «куда?»): {country_acc}
- Курорт: {resort}
- Отель: {hotel} {stars_str}
- Питание: {meal_ru}
- Вылет: {date}, {nights_str}
- Цена: от {price_str}/чел

ВАЖНО: используй правильные падежи. После «в» пиши винительный падеж ({country_acc}), после «из» — родительный, после «о/об» — предложный. Не пиши «тур в {country}» — правильно «тур в {country_acc}».

Структура поста (строго):
1. Первая строка — ОРИГИНАЛЬНЫЙ цепляющий заголовок в тегах <b>...</b>, с эмодзи в начале. Он должен быть:
   • уникальным (каждый раз новым — проявите креативность)
   • вызывать эмоцию: интригу, мечту, срочность, FOMO, любопытство
   • не шаблонным — избегайте «🔥 ГОРЯЩИЙ ТУР!», «Горячее предложение», «Успейте забронировать»
   • обращаться на «вы», не на «ты»
   Примеры разных стилей (для вдохновения, не копировать):
   — Интрига: «🤫 Нашли для вас кое-что интересное...»
   — FOMO:    «😱 Пока вы читаете — кто-то уже бронирует»
   — Мечта:   «🌊 Море, солнце, {meal_ru} — и это {country} ждёт»
   — Вопрос:  «🤔 Куда в отпуск? Вот ответ»
   — Эмоция:  «💫 {country} зовёт — и не отпустит»
   — Локал:   «✈️ Из Уфы прямо в {country_acc}»
   — Цифры:   «💸 {price_str}/чел — и вы в {country_acc}»
   — Отель:   «😍 {hotel} — и это цена?!»
2. Блок деталей: ✈️ вылет, 🏨 <b>название отеля</b>, 🍽 питание по-русски, 💰 <b>цена</b>
3. 1-2 атмосферных предложения об отдыхе
4. 3-4 преимущества с ✅
5. Две строки подряд (ОБЯЗАТЕЛЬНО обе):
   📩 Написать нам: <b>@hottourpegas_bot</b>
   📞 Позвонить: <b>+7 (917) 044-21-00</b>
6. ⚡ Количество мест ограничено!
7. Хэштеги: #горящийтур #{_country_tag(country)}

Стиль: живой, дружелюбный, как советует подруга. Без воды. Используй только HTML-теги <b> для жирного."""

        msg = client.messages.create(
            model="claude-haiku-4-5-20251001",
            max_tokens=500,
            messages=[{"role": "user", "content": prompt}],
        )
        post = msg.content[0].text.strip()
        if link_line:
            post += link_line
        return post
      except Exception:
        pass  # Если API недоступен — используем шаблон

    # Шаблонный вариант без ИИ — случайные описания (2-3 фразы)
    country_acc = _country_acc(country)
    desc_5_pool = [
        f"Роскошный отель премиум-класса в самом сердце {resort}. Ухоженная территория с бассейнами, ресторанами и садом создают атмосферу настоящего курорта. Идеально для тех, кто ценит высокий сервис и внимание к деталям.",
        f"Премиум-отель в {resort} — уровень, к которому быстро привыкаешь. Просторные номера, свежая еда, дружелюбный персонал. Здесь отдыхают те, кто знает, как должно быть.",
        f"{resort}, высшая категория отеля — отдых без компромиссов. SPA, несколько бассейнов, разнообразная кухня и анимация на любой вкус. Возвращаются сюда снова и снова — и есть за что.",
        f"Пятёрочный комфорт в {resort}: от встречи до проводов всё продумано. Красивый пляж, стильные номера и кухня, от которой не оторваться. Один из самых популярных отелей у наших клиентов.",
        f"Один из лучших отелей в {resort} — премиум-класс во всём. Расположение рядом с морем, ухоженная территория и уютные номера. Отдых, который хочется повторить.",
    ]
    desc_4_pool = [
        f"Отличный отель в {resort} с хорошим уровнем сервиса. Просторная территория, чистые номера, дружелюбный персонал. Прекрасный выбор для пар и семей с детьми.",
        f"Проверенный вариант в {resort} — комфортно, без излишеств и по делу. Хорошее питание, удобные номера, рядом пляж и магазины. Из тех отелей, куда возвращаются.",
        f"{resort}: четвёрка, которая стоит своих денег. Всё для спокойного отдыха — бассейн, ресторан, пляж в пешей доступности. Идеально для семейного отпуска.",
        f"Удобный отель в {resort} — всё необходимое, чтобы отдыхать, а не переживать. Приятная атмосфера, вкусная еда, красивый пляж. Хороший баланс цены и качества.",
        f"Комфортный отель в {resort} с большой территорией и бассейнами. Персонал говорит по-русски, кухня разнообразная. Отличный вариант для первого знакомства с {country_acc}.",
    ]
    desc_default_pool = [
        f"Комфортный отдых в {resort} по приятной цене. Всё необходимое для хорошего отпуска — чистые номера, питание, близость к пляжу. Отличный вариант, чтобы просто выдохнуть и отдохнуть.",
        f"{resort} — бюджетно и без лишних хлопот. Отель в шаговой доступности от моря, с приветливым персоналом и простой, но вкусной кухней. Для тех, кто ценит время на пляже больше интерьеров.",
        f"Недорогой вариант в {resort} — отдохнуть и не разориться. Всё самое нужное есть, а деньги останутся на экскурсии и сувениры. Отличный выбор для первого отпуска.",
        f"Простой и удобный отель в {resort} — главное впереди, на пляже. Чистые номера, шведский стол, бассейн — всё, что нужно для хорошего отпуска. Цена приятно удивит.",
    ]
    if stars.isdigit() and int(stars) >= 5:
        description = random.choice(desc_5_pool)
    elif stars.isdigit() and int(stars) >= 4:
        description = random.choice(desc_4_pool)
    else:
        description = random.choice(desc_default_pool)

    advantages = _pick_advantages(meal=meal, stars_digit=stars)
    advantages_str = "\n".join(advantages)

    return f"""<b>{headline}</b>

✈️ Вылет: {date} из Уфы ({nights_str})
🏨 <b>{hotel}</b> {stars_str}
🍽 Питание: {meal_ru}
💰 Цена: от <b>{price_str}/чел</b>

{description}

{advantages_str}{link_line}

📩 Написать нам: <b>@hottourpegas_bot</b>
📞 Позвонить: <b>+7 (917) 044-21-00</b>
⚡ Количество мест ограничено!

#горящийтур #{_country_tag(country)}"""


def generate_post_without_ai(tour: Tour) -> str:
    """
    Генерирует пост БЕЗ Claude API — только по шаблону.
    Используется как запасной вариант если нет API-ключа.
    """
    sea_line = f"🌊 До моря: {tour.distance_sea} м" if tour.distance_sea else ""
    country_acc = _country_acc(tour.country)

    desc_pool = [
        f"Отличный отдых в {country_acc} ждёт вас. Отель на проверенном курорте, всё необходимое для комфортного отпуска уже включено. Забирайте чемодан — и в путь.",
        f"{tour.country} — солнце, море и настоящий отпуск. Хороший отель, вкусная еда и побережье в шаговой доступности. Возвращаться не захочется.",
        f"Пора в отпуск: {tour.country} встречает уже совсем скоро. Ухоженный отель, тёплый климат, радушный приём. Всё как должно быть.",
        f"Тёплое море и ласковое солнце — всё это {tour.country}. Отель на популярном курорте с хорошей территорией и питанием. Просто отдых — без лишних забот.",
        f"{tour.country}: отдохнуть так, чтобы хватило впечатлений надолго. Удачное расположение отеля, красивые пляжи рядом и приветливый персонал. Возьмите с собой только хорошее настроение.",
    ]
    description = random.choice(desc_pool)

    advantages = _pick_advantages(
        meal=tour.meal,
        stars_digit=str(tour.hotel_stars) if tour.hotel_stars else "",
        sea_close=bool(tour.distance_sea and tour.distance_sea <= 100),
    )

    return POST_TEMPLATE.format(
        country          = tour.country,
        date_from        = tour.date_from,
        city_from        = tour.city_from,
        nights           = tour.nights,
        nights_word      = _nights_word(tour.nights),
        hotel_name       = tour.hotel_name,
        stars            = tour.stars_str,
        meal             = _meal_ru(tour.meal_label),
        sea_line         = sea_line,
        price_per_person = tour.formatted_price_per_person,
        ai_description   = description,
        ai_advantages    = "\n".join(advantages),
        country_tag      = _country_tag(tour.country),
    )
